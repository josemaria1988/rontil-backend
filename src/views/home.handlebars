<div>
    <h1>Lista de productos</h1>
    <ul>
      {{#each products}}
        <li>
          <h2>{{title}}</h2>
          <p>{{description}}</p>
          <p>Precio: <strong>${{price}}</strong></p>
          <p>Stock: <strong>{{stock}}</strong></p>
        </li>
      {{/each}}
    </ul>
</div>

